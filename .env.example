# ==========================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (Public)
# ==========================================
# These variables are exposed to the browser.
# Find them in Supabase Project Settings -> API
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ==========================================
# SUPABASE EDGE FUNCTION SECRETS (Private)
# ==========================================
# DO NOT add these to your .env file or expose them to the client.
# They are required exclusively for the backend Deno functions.
# Set them locally using the Supabase CLI:
# -> npx supabase secrets set KEY_NAME=value
#
# NEWSDATA_API_KEY=your_newsdata_api_key       # From newsdata.io (Environment news)
# OPENROUTER_API_KEY=your_openrouter_api_key   # From openrouter.ai (for AI tree verification)
# OPENAI_API_KEY=your_openai_api_key           # Alternate AI provider
# GFW_API_KEY=your_gfw_api_key                 # Global Forest Watch API
# NASA_FIRMS_API_KEY=your_nasa_firms_key       # NASA Active Fire Data
